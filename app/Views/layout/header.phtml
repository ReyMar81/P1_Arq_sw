<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Sistema de Control de Asistencia mediante QR">
    <title><?php echo isset($pageTitle) ? htmlspecialchars($pageTitle) . ' — AsistenciaQR' : 'AsistenciaQR'; ?></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        /* ===== VARIABLES ===== */
        :root {
            --primary:       #7c3aed;
            --primary-l:     #8b5cf6;
            --primary-glow:  rgba(124,58,237,.25);
            --secondary:     #06b6d4;
            --bg:            #0f172a;
            --surface:       #1e293b;
            --surface-h:     #263348;
            --glass:         rgba(255,255,255,.04);
            --border:        rgba(255,255,255,.08);
            --text:          #f1f5f9;
            --muted:         #94a3b8;
            --faint:         #475569;
            --success:       #10b981;
            --success-bg:    rgba(16,185,129,.1);
            --error:         #ef4444;
            --error-bg:      rgba(239,68,68,.1);
            --r:             12px;
            --r-sm:          8px;
            --nav-h:         64px;
            --tr:            all .15s ease;
        }

        /* ===== RESET ===== */
        *,*::before,*::after { box-sizing:border-box; margin:0; padding:0; }

        /* ===== BASE ===== */
        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg);
            color: var(--text);
            min-height: 100vh;
            font-size: 14px;
            line-height: 1.5;
            -webkit-font-smoothing: antialiased;
        }
        a { text-decoration: none; color: inherit; }

        /* ===== NAVBAR ===== */
        .navbar {
            height: var(--nav-h);
            background: var(--surface);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            padding: 0 20px;
            position: sticky;
            top: 0;
            z-index: 100;
            gap: 8px;
        }
        .nav-brand {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-right: 12px;
            flex-shrink: 0;
        }
        .nav-brand-icon {
            width: 36px; height: 36px;
            background: linear-gradient(135deg, var(--primary), var(--primary-l));
            border-radius: var(--r-sm);
            display: flex; align-items: center; justify-content: center;
            font-size: 18px;
        }
        .nav-brand-text { font-size: 15px; font-weight: 700; color: var(--text); white-space: nowrap; }

        .nav-sep { width:1px; height:22px; background:var(--border); margin:0 4px; flex-shrink:0; }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 2px;
            flex: 1;
            overflow-x: auto;
            scrollbar-width: none;
        }
        .nav-links::-webkit-scrollbar { display: none; }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 7px 12px;
            border-radius: var(--r-sm);
            font-size: 13px;
            font-weight: 500;
            color: var(--muted);
            white-space: nowrap;
            transition: var(--tr);
        }
        .nav-link:hover { color: var(--text); background: var(--glass); }
        .nav-link.active { color: #a78bfa; background: rgba(124,58,237,.12); }
        .nav-link .ni { font-size: 15px; line-height: 1; }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-left: auto;
            flex-shrink: 0;
        }
        .nav-user {
            display: flex;
            align-items: center;
            gap: 9px;
            padding: 5px 12px 5px 7px;
            background: var(--glass);
            border: 1px solid var(--border);
            border-radius: 100px;
        }
        .nav-avatar {
            width: 28px; height: 28px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 11px; font-weight: 700; color: #fff;
            flex-shrink: 0;
        }
        .nav-user-name  { font-size: 13px; font-weight: 600; color: var(--text); line-height: 1.2; }
        .nav-user-role  { font-size: 11px; color: var(--muted); text-transform: capitalize; }

        .btn-logout {
            display: flex; align-items: center; gap: 5px;
            padding: 7px 13px;
            border-radius: var(--r-sm);
            border: 1px solid var(--border);
            background: transparent;
            color: var(--muted);
            font-family: 'Inter', sans-serif;
            font-size: 13px; font-weight: 500;
            cursor: pointer;
            transition: var(--tr);
        }
        .btn-logout:hover { color: var(--error); border-color: rgba(239,68,68,.4); background: rgba(239,68,68,.06); }

        /* ===== LAYOUT ===== */
        .main-content { max-width: 1100px; margin: 0 auto; padding: 32px 24px; }

        /* ===== PAGE HEADER ===== */
        .page-header { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 24px; gap: 16px; flex-wrap: wrap; }
        .page-title  { font-size: 22px; font-weight: 700; color: var(--text); }
        .page-sub    { font-size: 13px; color: var(--muted); margin-top: 3px; }

        /* ===== ALERTS ===== */
        .alert {
            display: flex; align-items: center; gap: 10px;
            padding: 13px 16px;
            border-radius: var(--r);
            margin-bottom: 20px;
            font-size: 14px;
            animation: alertIn .25s ease;
        }
        @keyframes alertIn { from{opacity:0;transform:translateY(-6px)} to{opacity:1;transform:translateY(0)} }
        .alert-success { background: var(--success-bg); border:1px solid rgba(16,185,129,.25); color:#6ee7b7; }
        .alert-error   { background: var(--error-bg);   border:1px solid rgba(239,68,68,.25);  color:#fca5a5; }
        .alert-icon { font-size: 16px; flex-shrink: 0; }

        /* ===== CARD ===== */
        .card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r); overflow: hidden; }
        .card-header { display: flex; align-items: center; justify-content: space-between; padding: 18px 24px; border-bottom: 1px solid var(--border); }
        .card-title { font-size: 15px; font-weight: 600; color: var(--text); }
        .badge-count { font-size: 12px; font-weight: 600; padding: 3px 10px; border-radius: 100px; background: rgba(124,58,237,.12); color: #a78bfa; }

        /* ===== TABLE ===== */
        .tbl-wrap { overflow-x: auto; }
        .data-tbl { width: 100%; border-collapse: collapse; }
        .data-tbl th {
            padding: 12px 20px; font-size: 11px; font-weight: 600;
            letter-spacing: .06em; text-transform: uppercase;
            color: var(--muted); text-align: left;
            border-bottom: 1px solid var(--border);
            background: rgba(0,0,0,.12); white-space: nowrap;
        }
        .data-tbl td {
            padding: 15px 20px; font-size: 14px; color: var(--text);
            border-bottom: 1px solid var(--border); vertical-align: middle;
        }
        .data-tbl tbody tr:last-child td { border-bottom: none; }
        .data-tbl tbody tr { transition: background .1s ease; }
        .data-tbl tbody tr:hover td { background: var(--surface-h); }
        .td-num { color: var(--faint); font-size: 13px; width: 52px; }
        .td-actions { display: flex; align-items: center; gap: 6px; }

        /* ===== BUTTONS ===== */
        .btn {
            display: inline-flex; align-items: center; justify-content: center; gap: 6px;
            border: none; border-radius: var(--r-sm);
            font-family: 'Inter', sans-serif; font-weight: 500;
            cursor: pointer; text-decoration: none;
            transition: var(--tr); white-space: nowrap;
        }
        .btn-md  { padding: 9px 18px;  font-size: 14px; }
        .btn-sm  { padding: 6px 12px;  font-size: 12.5px; }
        .btn-primary { background: linear-gradient(135deg,var(--primary),var(--primary-l)); color:#fff; }
        .btn-primary:hover { transform:translateY(-1px); box-shadow:0 4px 16px var(--primary-glow); }
        .btn-primary:active { transform:translateY(0); }
        .btn-ghost  { background:var(--glass); color:var(--muted); border:1px solid var(--border); }
        .btn-ghost:hover { color:var(--text); background:var(--surface-h); }
        .btn-danger { background:rgba(239,68,68,.08); color:#f87171; border:1px solid rgba(239,68,68,.2); }
        .btn-danger:hover { background:rgba(239,68,68,.15); color:#fca5a5; }

        /* ===== EMPTY STATE ===== */
        .empty { padding:64px 24px; text-align:center; }
        .empty-icon  { font-size:48px; margin-bottom:16px; display:block; }
        .empty-title { font-size:16px; font-weight:600; color:var(--text); margin-bottom:6px; }
        .empty-desc  { font-size:14px; color:var(--muted); }

        /* ===== MODAL ===== */
        .overlay {
            display: none; position:fixed; inset:0;
            background: rgba(0,0,0,.65); backdrop-filter:blur(6px);
            z-index: 300; align-items:center; justify-content:center;
        }
        .overlay.open { display:flex; }
        .modal {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--r);
            width: 90%; max-width: 440px;
            padding: 28px;
            animation: modalIn .2s ease;
        }
        @keyframes modalIn { from{opacity:0;transform:scale(.96)} to{opacity:1;transform:scale(1)} }
        .modal-title { font-size:17px; font-weight:700; color:var(--text); margin-bottom:20px; }
        .modal-foot  { display:flex; gap:10px; justify-content:flex-end; margin-top:24px; }

        /* ===== FORM ===== */
        .form-group { margin-bottom: 18px; }
        .form-label {
            display: block; font-size:12px; font-weight:600;
            text-transform:uppercase; letter-spacing:.05em;
            color:var(--muted); margin-bottom:7px;
        }
        .form-ctrl {
            width: 100%;
            background: var(--bg);
            border: 1px solid var(--border);
            border-radius: var(--r-sm);
            padding: 11px 14px;
            font-size: 14px; color:var(--text);
            font-family:'Inter',sans-serif;
            outline: none; transition: var(--tr);
        }
        .form-ctrl:focus { border-color:var(--primary); box-shadow:0 0 0 3px rgba(124,58,237,.15); }
        .form-ctrl::placeholder { color:var(--faint); }

        /* ===== RESPONSIVE ===== */
        @media(max-width:640px) {
            .main-content { padding:20px 16px; }
            .nav-user-name,.nav-user-role { display:none; }
        }
    </style>
</head>
<body>
<?php
$usuario      = Auth::obtenerUsuario();
$currentPage  = isset($_GET['page']) ? $_GET['page'] : '';
$rol          = $usuario['rol'] ?? '';
$nombreFull   = trim(($usuario['nombre'] ?? '') . ' ' . ($usuario['apellido_p'] ?? ''));
$iniciales    = strtoupper(
    mb_substr($usuario['nombre'] ?? 'U', 0, 1) .
    mb_substr($usuario['apellido_p'] ?? '',  0, 1)
);
?>
<nav class="navbar">
    <a href="/?page=<?php echo $rol === 'docente' ? 'materia' : 'asistencia'; ?>" class="nav-brand">
        <div class="nav-brand-icon">📋</div>
        <span class="nav-brand-text">AsistenciaQR</span>
    </a>
    <div class="nav-sep"></div>

    <div class="nav-links">
        <?php if ($rol === 'docente'): ?>
            <a href="/?page=materia"             class="nav-link <?php echo $currentPage==='materia'             ? 'active' : ''; ?>"><span class="ni">📚</span> Materias</a>
            <a href="/?page=grupo"               class="nav-link <?php echo $currentPage==='grupo'               ? 'active' : ''; ?>"><span class="ni">👥</span> Grupos</a>
            <a href="/?page=registroEstudiante"  class="nav-link <?php echo $currentPage==='registroEstudiante'  ? 'active' : ''; ?>"><span class="ni">🎓</span> Estudiantes</a>
            <a href="/?page=clase"               class="nav-link <?php echo $currentPage==='clase'               ? 'active' : ''; ?>"><span class="ni">📅</span> Clases</a>
            <a href="/?page=qr"                  class="nav-link <?php echo $currentPage==='qr'                  ? 'active' : ''; ?>"><span class="ni">📱</span> Generar QR</a>
        <?php else: ?>
            <a href="/?page=asistencia" class="nav-link <?php echo $currentPage==='asistencia' ? 'active' : ''; ?>"><span class="ni">📋</span> Mi Asistencia</a>
        <?php endif; ?>
    </div>

    <div class="nav-right">
        <div class="nav-user">
            <div class="nav-avatar"><?php echo htmlspecialchars($iniciales); ?></div>
            <div>
                <div class="nav-user-name"><?php echo htmlspecialchars($nombreFull); ?></div>
                <div class="nav-user-role"><?php echo htmlspecialchars($rol); ?></div>
            </div>
        </div>
        <a href="/?page=auth&accion=logout" class="btn-logout">↩ Salir</a>
    </div>
</nav>

<div class="main-content">
